<?xml version="1.0" encoding="utf-8"?><!--
    MotionScene is a single XML file that describes an animation in a MotionLayout.
    Any views that are not animated may be constrained in the layout instead of the motion scene.

    MotionScene must have at least one transition.
    A transition must specify a start and end ConstraintSet for the transition. (Transition = One animation)
-->
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- A transition describes an animation via start and end state -->
    <Transition
        app:constraintSetEnd="@id/end"
        app:constraintSetStart="@id/start"
        app:duration="2000">

        <!-- MotionLayout will handle clicks on @id/red_star to "toggle" the animation between the start and end -->
        <OnClick
            app:clickAction="toggle"
            app:targetId="@id/red_star" />

    </Transition>

    <!-- Constraint to apply at the beginning of the animation -->
    <ConstraintSet android:id="@+id/start">
        <Constraint
            android:id="@+id/red_star"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </ConstraintSet>

    <!-- Constraint to apply at the end of the animation -->
    <ConstraintSet android:id="@+id/end">
        <Constraint
            android:id="@+id/red_star"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />
    </ConstraintSet>

</MotionScene>